{
  "name": "@ecosy/mailer",
  "version": "0.2.0",
  "description": "A production-ready email engine with a declarative template system, automatic retry with exponential backoff, and sliding-window rate limiting",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "eslint src/ && rollup -c && node scripts/check-dist.mjs && yarn examples:check",
    "examples:check": "tsc -p examples/tsconfig.json",
    "test": "yarn build && yarn test:run",
    "test:run": "node --test \"tests/*.test.mjs\"",
    "prepublishOnly": "yarn clean && yarn build && yarn test:run",
    "dev": "rollup -c --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/material-atomic/ecosy-mailer.git"
  },
  "keywords": [
    "ecosy",
    "mailer",
    "email",
    "template",
    "retry",
    "rate-limit",
    "smtp",
    "nodemailer",
    "typescript"
  ],
  "author": "material-atomic",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@rollup/plugin-terser": "^1.0.0",
    "@rollup/plugin-typescript": "^12.3.0",
    "@types/nodemailer": "^7.0.4",
    "@typescript-eslint/eslint-plugin": "^8.57.1",
    "@typescript-eslint/parser": "^8.57.1",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "glob": "^13.0.6",
    "nodemailer": "^10.0.10",
    "prettier": "^3.8.1",
    "rimraf": "^6.1.3",
    "rollup": "^4.59.1",
    "tslib": "^2.8.1",
    "typescript": "^5.9.3"
  },
  "sideEffects": false
}
